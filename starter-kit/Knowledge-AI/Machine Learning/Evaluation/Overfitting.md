# Overfitting

When a model memorizes training data and fails on new data: causes (capacity, noise, small samples), symptoms, and the standard remedies — regularization, more data, cross-validation, simpler models.
