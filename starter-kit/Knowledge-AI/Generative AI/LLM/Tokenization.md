# Tokenization

Turning text (or other signals) into sequences of discrete tokens that an LLM can consume. BPE, WordPiece, SentencePiece, and the trade-offs between vocabulary size, sequence length, and multilingual coverage.
