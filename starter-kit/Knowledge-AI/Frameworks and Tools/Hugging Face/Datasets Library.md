# Datasets Library

The `datasets` library: streaming, mapping, and shuffling large datasets, plus access to a hub of community datasets. Pairs with the [[Frameworks and Tools/Hugging Face/Transformers Library|Transformers Library]] for training and evaluation.
