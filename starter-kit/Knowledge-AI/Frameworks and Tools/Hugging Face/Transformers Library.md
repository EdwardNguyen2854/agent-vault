# Transformers Library

The `transformers` library: thousands of pretrained models (BERT, GPT, T5, LLaMA, Whisper, …), unified `AutoModel`/`AutoTokenizer` APIs, and a pipeline for quick inference. The standard interface to modern [[Deep Learning/Transformers/Transformers|transformer]] models.
