# Dataset and DataLoader

The PyTorch data pipeline: `Dataset` for per-sample access and transforms, `DataLoader` for batching, shuffling, parallel loading, and collation. The standard abstraction for feeding models.
