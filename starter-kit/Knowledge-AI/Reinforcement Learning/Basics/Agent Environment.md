# Agent Environment

The loop that defines RL: an agent observes the state of an environment, picks an action, and receives a new state and a reward. Everything else is a way of making that loop learn.
